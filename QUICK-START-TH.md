# 🚀 Quick Start - เริ่มต้นใช้งานด่วน (Python Version)

## สำหรับมือใหม่ที่ไม่เคยใช้ Playwright กับ Python มาก่อน

### ขั้นตอนที่ 1: ตรวจสอบว่าติดตั้ง Python แล้วหรือยัง

เปิด Command Prompt หรือ Terminal แล้วพิมพ์:

```bash
python --version
pip --version
```

ถ้าขึ้น error แสดงว่ายังไม่ได้ติดตั้ง Python → ไปดาวน์โหลดที่ https://www.python.org

**หมายเหตุ:** ต้องใช้ Python 3.8 หรือสูงกว่า

### ขั้นตอนที่ 2: เปิดโฟลเดอร์โปรเจค

```bash
cd c:\Users\Lenovo\Desktop\Test\Automated_testing\test-02
```

### ขั้นตอนที่ 3: สร้าง Virtual Environment (แนะนำ)

```bash
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน (Windows)
venv\Scripts\activate

# เปิดใช้งาน (Linux/Mac)
source venv/bin/activate
```

เมื่อเปิดใช้งานสำเร็จ จะเห็น `(venv)` ข้างหน้าใน command line

### ขั้นตอนที่ 4: ติดตั้ง Dependencies (ทำครั้งแรกครั้งเดียว)

```bash
# ติดตั้ง Python packages
pip install -r requirements.txt

# ติดตั้ง Playwright browsers
playwright install
```

รอจนกว่าจะเสร็จ (อาจใช้เวลา 5-10 นาที)

### ขั้นตอนที่ 5: รัน Test ครั้งแรก

**วิธีที่ 1: รัน test ง่ายๆ ก่อน (แนะนำ!)**
```bash
pytest tests/test_example_quick.py -v --headed
```

**วิธีที่ 2: รัน login tests**
```bash
pytest tests/test_login.py -v --headed
```

**วิธีที่ 3: รัน test ทั้งหมด**
```bash
pytest -v
```

### ขั้นตอนที่ 6: ดู Test Report

```bash
# สร้าง HTML report
pytest --html=report.html --self-contained-html

# เปิดไฟล์ report.html ใน browser
```

## คำสั่งที่ใช้บ่อย

### 🎯 รัน Test แบบต่างๆ

```bash
# รัน test ทั้งหมด
pytest

# รัน test แบบเห็น browser (แนะนำสำหรับมือใหม่!)
pytest --headed -v

# รัน test แบบ slow motion (เห็นการทำงานชัดเจน)
pytest --headed --slowmo=1000 -v

# รัน test เฉพาะไฟล์เดียว
pytest tests/test_login.py -v

# รัน test เฉพาะ class
pytest tests/test_login.py::TestLogin -v

# รัน test เฉพาะ function
pytest tests/test_login.py::TestLogin::test_login_successfully_with_standard_user -v

# รัน test ที่มี keyword
pytest -k "login" -v
```

### 📊 รัน Test ตาม Marker (Tags)

```bash
# รัน test ที่เป็น login tests
pytest -m login -v

# รัน test ที่เป็น smoke tests
pytest -m smoke -v

# รัน test ที่เป็น e2e tests
pytest -m e2e -v

# รัน test ที่เป็น cart tests
pytest -m cart -v
```

### 🌐 รัน Test บน Browser ต่างๆ

```bash
# Chromium (Chrome)
pytest --browser chromium -v

# Firefox
pytest --browser firefox -v

# WebKit (Safari)
pytest --browser webkit -v

# รันบนทุก browsers พร้อมกัน
pytest --browser chromium --browser firefox --browser webkit -v
```

### ⚡ รัน Test แบบ Parallel (เร็วขึ้น)

```bash
# ติดตั้ง pytest-xdist ก่อน
pip install pytest-xdist

# รันแบบ parallel (เร็วมาก!)
pytest -n auto -v
```

### 📸 รัน Test พร้อมสร้าง Report

```bash
# HTML Report
pytest --html=report.html --self-contained-html -v

# Screenshot เมื่อ test ล้มเหลว (default อยู่แล้ว)
pytest --screenshot=only-on-failure -v

# Video เมื่อ test ล้มเหลว (default อยู่แล้ว)
pytest --video=retain-on-failure -v
```

### 🐛 Debug Tests

```bash
# รันแบบ debug (หยุดเมื่อ test ล้มเหลว)
pytest --pdb tests/test_login.py -v

# รันแบบ slow motion (ช้าลงเพื่อดูการทำงาน)
pytest --headed --slowmo=2000 tests/test_login.py -v

# เปิด Playwright Inspector (Windows PowerShell)
$env:PWDEBUG=1
pytest tests/test_login.py

# เปิด Playwright Inspector (Linux/Mac)
PWDEBUG=1 pytest tests/test_login.py
```

## โครงสร้างโปรเจคแบบง่าย

```
test-02/
├── tests/                          👈 โฟลเดอร์เก็บไฟล์ test
│   ├── __init__.py                👈 Python package file
│   ├── test_login.py              👈 ทดสอบการ Login
│   ├── test_products.py           👈 ทดสอบหน้าสินค้า
│   ├── test_cart.py               👈 ทดสอบตะกร้า
│   ├── test_checkout.py           👈 ทดสอบการชำระเงิน
│   ├── test_e2e_complete_flow.py  👈 ทดสอบ End-to-End
│   └── test_example_quick.py      👈 ตัวอย่างสำหรับเริ่มต้น
├── venv/                           👈 Virtual environment
├── conftest.py                     👈 Pytest configuration & fixtures
├── pytest.ini                      👈 ไฟล์ตั้งค่า pytest
├── requirements.txt                👈 Python dependencies
├── .gitignore                      👈 Git ignore rules
├── README.md                       👈 เอกสารหลัก
└── QUICK-START-TH.md              👈 ไฟล์นี้
```

## ไฟล์ Test ที่มีในโปรเจค

| ไฟล์ | จำนวน Tests | คำอธิบาย |
|------|-------------|----------|
| `test_login.py` | 7 tests | ทดสอบการ Login |
| `test_products.py` | 9 tests | ทดสอบหน้าสินค้า |
| `test_cart.py` | 11 tests | ทดสอบตะกร้าสินค้า |
| `test_checkout.py` | 13 tests | ทดสอบการชำระเงิน |
| `test_e2e_complete_flow.py` | 7 tests | ทดสอบ End-to-End |
| `test_example_quick.py` | 7 tests | ตัวอย่างสำหรับเรียนรู้ |

**รวมทั้งหมด: 54 test cases**

## Tips สำหรับมือใหม่

### 1. เริ่มต้นด้วย Example Tests

```bash
# รัน test ตัวอย่างแรก
pytest tests/test_example_quick.py::TestQuickExamples::test_example_1_check_website_title -v --headed
```

เรียนรู้จากตัวอย่างง่ายๆ ก่อน!

### 2. ใช้ --headed เพื่อดูการทำงาน

```bash
pytest tests/test_login.py -v --headed
```

จะได้เห็น browser เปิดและทำงานจริง

### 3. ใช้ --slowmo เพื่อช замедลить

```bash
pytest tests/test_login.py -v --headed --slowmo=1000
```

จะได้เห็นแต่ละ step ชัดเจน (หน่วงเวลา 1000ms ต่อ action)

### 4. ใช้ -v หรือ -vv เพื่อดู Output ละเอียด

```bash
# Verbose
pytest tests/test_login.py -v

# Very Verbose
pytest tests/test_login.py -vv
```

### 5. ใช้ -k เพื่อค้นหา Test

```bash
# รัน test ที่มีคำว่า "successfully"
pytest -k "successfully" -v

# รัน test ที่มีคำว่า "error"
pytest -k "error" -v
```

### 6. ใช้ --pdb เมื่อต้องการ Debug

```bash
pytest tests/test_login.py --pdb
```

จะหยุดที่ test ที่ล้มเหลวและให้ใช้ Python debugger

## การแก้ปัญหาเบื้องต้น

### ปัญหา: ไม่สามารถรันคำสั่ง pytest ได้

**แก้ไข:** ตรวจสอบว่าติดตั้ง dependencies แล้ว
```bash
pip install -r requirements.txt
```

### ปัญหา: "playwright: command not found"

**แก้ไข:** ติดตั้ง Playwright browsers
```bash
playwright install
```

### ปัญหา: Test timeout

**แก้ไข:** เพิ่ม timeout ใน pytest.ini หรือใช้ command
```bash
pytest --timeout=60 tests/test_login.py -v
```

### ปัญหา: Test ล้มเหลว

**แก้ไข:** รันแบบ debug เพื่อดูว่าเกิดอะไรขึ้น
```bash
pytest tests/test_login.py --pdb --headed --slowmo=1000 -v
```

### ปัญหา: Import Error หรือ Module Not Found

**แก้ไข:** ตรวจสอบว่าเปิดใช้ virtual environment แล้ว
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### ปัญหา: หน้าเว็บโหลดช้า

**แก้ไข:** เพิ่ม wait หรือใช้ performance_glitch_user
```python
page.wait_for_load_state("networkidle")
```

## ลำดับการเรียนรู้แนะนำ

### สัปดาห์ที่ 1: พื้นฐาน
1. รัน test ตัวอย่าง (`test_example_quick.py`)
2. ดู test report
3. ทดลองรันแบบ --headed และ --slowmo
4. อ่านโค้ดในไฟล์ `test_example_quick.py`

### สัปดาห์ที่ 2: ทำความเข้าใจ
1. อ่านโค้ดใน `test_login.py` ทีละ test
2. ทดลองแก้ไขโค้ดเล็กน้อย
3. ลองเพิ่ม `print()` เพื่อดูค่าต่างๆ
4. ทดลองทำให้ test ล้มเหลวดู
5. เรียนรู้เกี่ยวกับ fixtures ใน `conftest.py`

### สัปดาห์ที่ 3: เขียนเอง
1. ลองเขียน test ง่ายๆ เพิ่มเติมใน `test_example_quick.py`
2. ทดลองใช้ fixtures
3. สร้าง test file ใหม่ของตัวเอง
4. ลองเขียน test สำหรับเว็บไซต์อื่น

## ทรัพยากรเพิ่มเติม

### เอกสารภาษาไทย
- 📄 `README.md` - เอกสารหลักภาษาไทย
- 📄 `QUICK-START-TH.md` - ไฟล์นี้

### ไฟล์ตัวอย่าง
- 📝 `tests/test_example_quick.py` - ตัวอย่างพื้นฐาน
- 📝 `conftest.py` - ตัวอย่าง fixtures และ configuration

### เว็บไซต์
- 🌐 [Playwright Python Docs](https://playwright.dev/python/)
- 🌐 [Pytest Documentation](https://docs.pytest.org/)
- 🌐 [Python Official Tutorial](https://docs.python.org/3/tutorial/)

## ตัวอย่างการใช้งานจริง

### ตัวอย่าง 1: รัน Test เฉพาะ Login

```bash
# 1. เปิด virtual environment
venv\Scripts\activate

# 2. รัน login tests แบบเห็น browser
pytest tests/test_login.py -v --headed

# 3. ดู report
pytest tests/test_login.py --html=login-report.html --self-contained-html
```

### ตัวอย่าง 2: รัน Smoke Tests เท่านั้น

```bash
pytest -m smoke -v --headed
```

### ตัวอย่าง 3: Debug Test ที่ล้มเหลว

```bash
# รันแบบ slow และหยุดเมื่อ error
pytest tests/test_login.py --pdb --headed --slowmo=2000 -v
```

### ตัวอย่าง 4: รัน Test บนหลาย Browsers

```bash
pytest tests/test_login.py --browser chromium --browser firefox -v
```

## คำแนะนำสุดท้าย

1. **อย่ากลัวที่จะลองผิดลองถูก** - Test ล้มเหลวเป็นเรื่องปกติ
2. **ใช้ --headed และ --slowmo บ่อยๆ** - มันช่วยให้เข้าใจง่ายขึ้นมาก
3. **อ่านโค้ดที่มีอยู่** - เรียนรู้จากตัวอย่างที่มีอยู่แล้ว
4. **ทดลองแก้ไข** - แก้โค้ดเล็กๆ น้อยๆ แล้วรันดู
5. **ใช้ print()** - พิมพ์ค่าออกมาดูเพื่อเข้าใจ
6. **ใช้ Virtual Environment** - ป้องกันปัญหา dependency conflicts

---

**เริ่มต้นเลยตอนนี้!**

```bash
# 1. เปิด virtual environment
venv\Scripts\activate

# 2. รัน test ตัวอย่างแรก
pytest tests/test_example_quick.py -v --headed --slowmo=1000

# 3. สนุกกับการเรียนรู้! 🎉
```

**Happy Testing! 🐍🎭**
